/*
 * ToyView Messages
 *
 * This contains all the text for the ToyView container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ToyView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ToyView container!',
  },
});
