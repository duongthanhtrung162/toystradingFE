/*
 * ToyListPage Messages
 *
 * This contains all the text for the ToyListPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ToyListPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ToyListPage container!',
  },
});
