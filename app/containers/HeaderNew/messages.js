/*
 * HeaderNew Messages
 *
 * This contains all the text for the HeaderNew container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HeaderNew';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HeaderNew container!',
  },
});
