// import { selectSoldListPageDomain } from '../selectors';

describe('selectSoldListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
