/*
 * UserView Messages
 *
 * This contains all the text for the UserView container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserView container!',
  },
});
