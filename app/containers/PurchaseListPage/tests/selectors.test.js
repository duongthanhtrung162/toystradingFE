// import { selectPurchaseListPageDomain } from '../selectors';

describe('selectPurchaseListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
