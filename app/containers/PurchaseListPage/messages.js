/*
 * PurchaseListPage Messages
 *
 * This contains all the text for the PurchaseListPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PurchaseListPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PurchaseListPage container!',
  },
});
