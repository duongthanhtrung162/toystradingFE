/*
 * HomeAdmin Messages
 *
 * This contains all the text for the HomeAdmin container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HomeAdmin';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HomeAdmin container!',
  },
});
