/*
 * ProductDetailPage Messages
 *
 * This contains all the text for the ProductDetailPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProductDetailPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProductDetailPage container!',
  },
});
