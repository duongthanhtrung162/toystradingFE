/*
 * TagView Messages
 *
 * This contains all the text for the TagView container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TagView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TagView container!',
  },
});
