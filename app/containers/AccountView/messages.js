/*
 * AccountView Messages
 *
 * This contains all the text for the AccountView container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AccountView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AccountView container!',
  },
});
