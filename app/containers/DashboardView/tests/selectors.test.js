// import { selectDashboardViewDomain } from '../selectors';

describe('selectDashboardViewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
