/*
 * ToyPageEdit Messages
 *
 * This contains all the text for the ToyPageEdit container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ToyPageEdit';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ToyPageEdit container!',
  },
});
